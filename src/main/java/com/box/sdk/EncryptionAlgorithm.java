package com.box.sdk;

/** Enum to identify algorithm for JWT. */
public enum EncryptionAlgorithm {

  /** RSA with SHA 256. */
  RSA_SHA_256,

  /** RSA with SHA 384. */
  RSA_SHA_384,

  /** RSA with SHA 512. */
  RSA_SHA_512;
}
